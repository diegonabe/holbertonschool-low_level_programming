#include "main.h"
/**
 * Esta función retorna el largo del string
 *
 * RETURN 0 (Succcess)
 */

int _strlen(char *s)
{
	int contador;
	for (contador = 0; s[contador] != '\0'; contador++)
	{
	}	
	return (contador);
	
}

